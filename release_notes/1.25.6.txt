Release Notes 1.25.6
Released: 2026-09-15

Changes:

- Release-Notes fuer 1.25.5 ergaenzen [skip ci]

- Prepare next development iteration (1.25.6-SNAPSHOT)

- Raumbelegungen-Report: Tabellen-Kopfzeile auf jeder Druckseite wiederholen (#696)
  Übernimmt dasselbe, bereits etablierte Muster aus den übrigen Report-
  Komponenten (z.B. UebersichtRaeume.vue, FreieSlotsReferenten.vue):
  page-break-inside: avoid auf jeder Zeile, damit keine Zeile über einen
  Seitenumbruch gerissen wird, plus explizites
  thead { display: table-header-group; } für robustes Verhalten über
  Browser hinweg.
  Verifiziert per Playwright-generiertem Druck-PDF (künstlich kleine
  Seitenhöhe, um Seitenumbrüche zu erzwingen) gegen die echte
  Komponente inkl. echter Report-Daten: die Kopfzeile (Zeit/Vortrag/
  Referent/Teilnehmer) erscheint zuverlässig auf jeder neuen Seite,
  sobald die Belegung eines Raums über eine Seite hinausgeht.

- Release 1.25.6
  Enthält den Fix für #696: Tabellen-Kopfzeile im Raumbelegungen-Report
  wird beim Drucken jetzt auf jeder neuen Seite wiederholt (#697).

